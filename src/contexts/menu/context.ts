/* eslint-disable @typescript-eslint/no-explicit-any */
import { createMenuContext } from 'minimal-components-react/dist/contexts/menu';

const MenuContext = createMenuContext<any>();

export { MenuContext };
